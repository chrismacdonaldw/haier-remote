import 'package:flutter/material.dart';

abstract class RemoteButton extends StatelessWidget {
  const RemoteButton({Key key, List<int> sensorInfo}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: SizedBox(),
    );
  }
}
